"use strict";jest.dontMock("../source/javascripts/components/heat.jsx");

describe("Heatsink", function () {

  it("Heatsink count is pre-set when loaded", function () {
    var React = require("react");
    var TestUtils = require("react-addons-test-utils");
    var HeatSink = require("../source/javascripts/components/heatsink.jsx");

    // Render a Heat component into the document
    var heatsink_component = TestUtils.renderIntoDocument(
    React.createElement(HeatSink, null));


    expect(heatsink.state.internal_heatsinks).toEqual(10);
    expect(heatsink.state.external_heatsinks).toEqual(4);
    expect(heatsink.state.double_heatsinks).toEqual(false);});});